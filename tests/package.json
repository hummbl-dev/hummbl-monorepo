{
  "name": "@hummbl/tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "echo 'Tests disabled - E2E tests need fixing'",
    "test:watch": "vitest",
    "test:e2e": "echo 'E2E tests disabled - need API fixes'",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@hummbl/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.25.2"
  },
  "devDependencies": {
    "vitest": "^4.0.16",
    "@vitest/coverage-v8": "^4.0.16"
  }
}